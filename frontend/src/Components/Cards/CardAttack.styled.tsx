import {createUseStyles} from 'react-jss';

export const useStyles = createUseStyles({
  types: {
    textAlign: 'center',
    color: '#38424d',
    fontSize: '0.8rem',
    fontWeight: 100,
  },
  damage: {
    fontWeight: 600,
  },
});
